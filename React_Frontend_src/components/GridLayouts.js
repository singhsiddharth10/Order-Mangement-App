import React from 'react';
import GridHeader from './GridHeader'
import GridTable from './GridTable'

//in this js file I am only rendering two component name as grid header and grid table
  function GridLayouts() {
    return (
      <div>
        <GridHeader/>
        <GridTable/>
      </div>
    );
  }
  export default GridLayouts
  